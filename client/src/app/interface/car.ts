// Interface to define the structure of a Car object
export interface Car {
    id?: number,
    brand_name: string,
    model_name: string,
    year: number,
    cilindrical_capacity: number
    tax: number
};